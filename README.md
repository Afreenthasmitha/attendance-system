# attendance-system
A client–server based attendance system where users log in via a web interface to mark attendance. The frontend sends requests to a Flask backend, which validates users and stores timestamps in a database. It demonstrates real-time communication, authentication, and efficient data management.
